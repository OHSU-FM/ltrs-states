<div class="container" style="width: inherit;">
  <div class="row">
    <div class="col-lg-12">
      <h3>Traveler Details</h3>
      <% if reimbursement_request.new_record? %>
        <% if current_user.has_delegators? %>
        <div class="ui-widget-content ui-corner-all wide partial_form">
          <%= f.label :user_id, 'Submit request for:' %><br/>
          <%= f.select :user_id, options_from_collection_for_select([current_user] + current_user.delegators, "id", "full_name", current_user.id) %>
        </div>
        <% else %>
          <p>Submitting Travel Request as: <%= current_user.full_name %></p>
        <% end %>
      <% else %>
        <% if reimbursement_request.form_email != reimbursement_request.user.email %>
          <div class="ui-widget-content ui-corner-all wide partial_form">
            Submitted for:
            <%= reimbursement_request.user.full_name %>
            <br/>
            <b>Submitted by</b>
            <%= reimbursement_request.form_user %>
          </div>
        <% else %>
          <p>Travel Request for <%= reimbursement_request.user.full_name %></p>
        <% end %>
      <% end %>
    </div>
  </div>
</div>

<div class="container" style="width: inherit;">
  <div class="row">
      <h3>Travel Event Details</h3>
      <div class="col-lg-6">
        <div class="row">
          other FMR?
        </div>

        <div class="row">
          attachments
        </div>
      </div>
      <div class="col-lg-6">
        <div class="row">
          <p>Departure date: <%= reimbursement_request.depart_date %></p>
          <br />
          <p>Return date: <%= reimbursement_request.return_date %></p>
        </div>

        <div class="row"></div>
      </div>
    </div>
  </div>
</div>

<div class="container" style="width: inherit;">
  <div class="row">
      <h3>Event & Travel Reimbursements</h3>
      <div class="col-lg-12">
        meal reimb
      </div>

      <div class="col-lg-6">
        <div class="row">
          <p>Is traveler submitting for airline reimbursement? <% reimbursement_request.air_use %></p>
        </div>

        <div class="row">
          <p>Is traveler submitting for lodging reimbursement? <% reimbursement_request.lodging_reimb %></p>
        </div>

        <div class="row">
          <p>Is traveler submitting for mileage reimbursement? <% reimbursement_request.traveler_mileage_reimb %></p>
        </div>
      </div>
      <div class="col-lg-6">
        <div class="row">
          <p>Is traveler submitting for rental car reimbursement? <% reimbursement_request.car_rental %></p>
        </div>

        <div class="row">
          <p>Is traveler submitting for meal hosting reimbursement? <em>TODO</em></p>
        </div>
      </div>
    </div>
  </div>
</div>

